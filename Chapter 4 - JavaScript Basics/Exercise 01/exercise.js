/* 
    Brand, model, and prodcutionYear are const because I assumed they can't be changed.
    While ownerName and isNew can be changed so I declared them as let. 
*/

const brand = "BMW";
const model = "i8 M";
const productionYear = 2024;
let ownersName = "Someone";

let isNew = true;